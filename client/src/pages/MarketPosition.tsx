
import { Layout } from "@/components/layout/Layout";
import { MarketPositionAnalysis } from "@/components/market-position/MarketPositionAnalysis";

const MarketPosition = () => {
  return (
    <Layout>
      <MarketPositionAnalysis />
    </Layout>
  );
};

export default MarketPosition;
