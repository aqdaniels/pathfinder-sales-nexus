
import { Layout } from "@/components/layout/Layout";
import { ValueChainAnalysis } from "@/components/value-chain/ValueChainAnalysis";

export default function ValueChain() {
  return (
    <Layout>
      <ValueChainAnalysis />
    </Layout>
  );
}
